# {{ID}} — spec

> Owner per `spec.config.json.roles`. Describes *what* and *why*, not *how* (that is `tech.md`).
> Always the **current** behaviour of this capability. Quality bar: business language only (no paths/symbols),
> full lifecycle coverage, per-segment flows, exact numbers + a worked example. See `rules/spec.md`.

## Problem / context
<!-- Why does this capability exist? What user pain does it solve? -->
-

## Requirements
<!-- Addressable requirement blocks. Deltas fold in by matching "### Requirement: <title>". -->

### Requirement: <title>
The system SHALL <behaviour>.

#### Scenario: <name>
- WHEN <trigger>
- THEN <outcome>

## Business rules
<!-- Limits, gating, quotas, defaults, formulas (one worked example each). -->
-

## Use cases
<!-- Concrete scenarios: actor → action → outcome. -->
1.

## UI / Design
- **Design link:** <URL or `ui.html`, mirrored in meta.yml design/ui>
- **Screens / states:** <key screens and states>
- **Notable interactions:** <hover, selected, disabled, loading, error, empty>

## Out of scope
<!-- Explicitly what this capability does NOT do. -->
-
