# {{ID}} — technical notes

> Owner per `spec.config.json.roles`. *How* it is built. Links spec ↔ code. Describe the **design** (architecture /
> patterns), not just a file list — files move, design persists. Volatile `repo:path:line` anchors live in the
> Traceability map at the end. See `rules/tech.md`.

## Design overview / Architecture
<!-- THE most important section. Pattern / structure. If several variants share one skeleton, describe the common
     frame + what each variant adds. Draw a diagram when clearer than prose. -->
-

## Data model
<!-- Tables + columns / storage touched. A column usually encodes a rule — name it. -->
-

## Flow
<!-- Trigger → effect. -->
1.

## External APIs / integrations
-

## Risks / gotchas
-

## Notes for AI codegen
<!-- Conventions to follow, things to NOT touch, existing helpers to reuse. -->
-

## Traceability map
<!-- REQUIRED, ends the file: each spec rule/section → repo:path:line (or symbol). Carries the volatile anchors. -->
| Spec rule / section | Code anchor(s) |
| --- | --- |
| | |
